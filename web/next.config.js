/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Environment variables are automatically available in Next.js 15
  // No need to explicitly list them in env config
}

module.exports = nextConfig

